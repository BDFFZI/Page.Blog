创建：
create function {functionName}([{parameterName} {type}][,]) 
returns {type}
deterministic
begin
    [functionBody]
    [return({value})]
end


调用：
select {functionName}([{parameterValue}][,]);

删除：
drop function [if exists] {functionName};

//函数体参见储存过程
